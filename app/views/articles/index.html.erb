<h1>
  <%= @category.name %>
</h1>

<% @articles.each do |article| %>
  <div>
    <h2><%= link_to article.title, article_url(article.id) %></h2>
    <p><%= article.body[0..100] %></p>
  </div>
<% end %>