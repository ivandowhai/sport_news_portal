h1
  | Editing page
= render 'layouts/categories_list'
= form_with(model: @page, url: "/admin/pages/#{@page.id}", method: 'PUT') do |form|
  - if @page.errors.any?
    #error_explanation
      h2
        = pluralize(@page.errors.count, "error")
        |  prohibited this article from being saved:
      ul
        - @page.errors.each do |error|
          li
            = error.full_message
  .form-group
    = form.label :name, class: 'label'
    = form.text_field :name, class: 'form-control'
  .form-group
    = form.label :body, class: 'label'
    = form.text_area :body, class: 'form-control'
  .form-group
    = form.label :priority, class: 'label'
    = form.text_field :priority, class: 'form-control'
  .form-group
    = form.label :page_category_id, class: 'label'
    = form.select :page_category_id, options_for_select(@categories, @page.page_category_id), {}, class: 'form-control'
  .form-group
    = form.submit nil, class: 'btn btn-primary'
